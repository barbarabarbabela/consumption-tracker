{
  "type": "postgres",
  "host": "localhost",
  "port": "${POSTGRES_PORT}",
  "username": "${POSTGRES_USER}",
  "password": "${POSTGRES_PASSWORD}",
  "database": "${POSTGRES_NAME}",
  "entities": ["src/entity/*.ts"]
}
